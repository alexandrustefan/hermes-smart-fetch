# hermes-smart-fetch — Hermes Agent plugin manifest
#
# Install: hermes plugins install /path/to/hermes-smart-fetch --enable
# Or symlink: ln -s /path/to/hermes-smart-fetch ~/.hermes/plugins/hermes-smart-fetch

name: hermes-smart-fetch
version: 0.1.0
description: >
  Local web extraction with browser-grade TLS fingerprinting and Defuddle
  content cleanup. No API key required — uses @thinkscape/smart-fetch CLI.
author: alexandrustefan
license: MIT
kind: backend  # Auto-loaded by the plugin system

# The register() function in __init__.py handles all registration.
# No additional entry point configuration needed.
